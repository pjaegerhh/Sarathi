it seems when a user clicks on a profile of a different user - in community for example - the profile shown is not responsive and mobile optimized. check this and change

now work on own profile. when on own profile and clicking on a connection in "My Connections" the profile of the clicked user should show up

it seems that a user during first time onboarding can only chose "Amputee" in step 7 User Type selection. make sure a user and chose any usertype. 

Profile verified call back after signup is not responsive and not mobile optimized change this

when doing the complete onboarding and on last page of process "Explore Sarathi" button is clicked the user should be directed to login page to get a real session